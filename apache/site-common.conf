# =============================================================================
# apache/site-common.conf — static Jekyll site rules (included in a vhost).
# Modules: mod_headers, mod_deflate, mod_expires, mod_rewrite (optional)
# =============================================================================
Include ${INFOPS_CONF_DIR}/security-headers.conf

<Directory "${INFOPS_SITE_ROOT}">
    Options -Indexes -Includes -ExecCGI +FollowSymLinks
    AllowOverride None
    Require all granted
    DirectoryIndex index.html
</Directory>

# Hidden files (.git, .env, .htaccess…) are never served
<LocationMatch "/\.(?!well-known/)">
    Require all denied
</LocationMatch>

# Jekyll pretty URLs: /about → /about.html when it exists
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} !-f
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI} !-d
    RewriteCond %{DOCUMENT_ROOT}%{REQUEST_URI}.html -f
    RewriteRule ^(.+?)/?$ $1.html [L]
</IfModule>

ErrorDocument 404 /404.html

# WebAssembly (GBA emulator in the terminal): the right type lets browsers
# compile it while it downloads
AddType application/wasm .wasm

# GBA emulator ROM library (folder mounted by docker-compose): plain downloads,
# no directory listing, never indexed; the list itself is always re-read
<Location "/assets/roms/">
    Options -Indexes
    ForceType application/octet-stream
    <IfModule mod_headers.c>
        Header set X-Robots-Tag "noindex, nofollow"
        Header set Cache-Control "public, max-age=86400"
    </IfModule>
</Location>
<Location "/assets/roms/roms.json">
    ForceType application/json
    <IfModule mod_headers.c>
        Header set Cache-Control "no-cache"
    </IfModule>
</Location>

<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/css text/javascript application/javascript application/wasm application/json application/xml application/rss+xml application/atom+xml image/svg+xml
</IfModule>

# Assets are not fingerprinted → moderate cache; feeds/search index → short
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresDefault                          "access plus 1 hour"
    ExpiresByType text/html                 "access plus 0 seconds"
    ExpiresByType text/css                  "access plus 30 days"
    ExpiresByType text/javascript           "access plus 30 days"
    ExpiresByType application/javascript    "access plus 30 days"
    ExpiresByType image/svg+xml             "access plus 30 days"
    ExpiresByType image/png                 "access plus 30 days"
    ExpiresByType image/jpeg                "access plus 30 days"
    ExpiresByType image/webp                "access plus 30 days"
    ExpiresByType font/woff2                "access plus 30 days"
    ExpiresByType application/wasm          "access plus 30 days"
    ExpiresByType application/xml           "access plus 1 hour"
    ExpiresByType application/json          "access plus 1 hour"
</IfModule>
